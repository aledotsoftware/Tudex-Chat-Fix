#!/bin/bash
sed -i 's/- \*\*ChatFix-UX-Accessibility-Trust\*\*: Accesibilidad operativa, manejo de errores de estado y focus de elementos mejorados en frontend. Se han removido outline anti-patterns y mejorado las etiquetas semánticas. Mejorado el feedback visual de los estados de conexión (desconectado, conectando) usando nuevos banners no bloqueantes (infoBanner, warningBanner) que respetan la accesibilidad (WCAG) en PWA, reduciendo el bloqueo de la interfaz a estados estrictos (QR, auth_failure). Actualizada la legibilidad del texto en banners de conexión y estados de error. Optimizado el estilo visual del box-shadow de focus en inputs con `var(--accent-glow)`. Los errores de login ahora se comunican correctamente en vivo vía `aria-live="assertive"`./- \*\*ChatFix-UX-Accessibility-Trust\*\*: Accesibilidad operativa consolidada en Frontend. Se previno el autocorrect y autocapitalize en inputs técnicos (API Keys, Tokens) para evitar interferencia del teclado móvil. Se resolvieron conflictos de `aria-live` en los componentes `.loadingSpinnerContainer`, moviendo los atributos de ARIA a los contenedores externos y usando `aria-live="polite"` y `assertive` sin superposición de roles `alert` en los hijos. Se mejoró la visibilidad y el contraste de los `buttonSpinner` con fondos oscuros. Se implementó `aria-pressed` en los botones de "mostrar\/ocultar contraseña"./g' .jaa/state.md
