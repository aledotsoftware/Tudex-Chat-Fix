# JAA Global System State

Este archivo contiene el estado compartido entre todos los repositorios gestionados por JAA.
Los agentes pueden leer este estado para entender el contexto de otros proyectos.

## 🚀 ACTIVE MILESTONES
- [JAA] Implementación de Jerarquía de Contexto (.jaa.md global) - **COMPLETADO**
- [JAA] Sistema de Estado Global (system-state.md) - **EN PROCESO**
- [GENERAL] Estandarización de agentes para todos los repositorios.
- [ChatFix] Accesibilidad, Confianza y Feedback en UI completado (App.jsx, App.css) - **COMPLETADO**
- [ChatFix] Operaciones y Configuración IA (backend/index.js) - **COMPLETADO**
- [ChatFix] Validación y Coordinación Arquitectónica - **COMPLETADO**

## 📝 AGENT NOTES
- **ChatFix-Provider-Bridge**: Fixed `markRead` flow in `backend/index.js` by explicitly passing `conversationId: chatId` to the provider adapter when called from the specific chat route, adhering to the canonical parameter contract. Tests and builds pass.
- **ChatFix-Orchestrator**: Validación final de la Arquitectura de Mensajería Centralizada completada exitosamente. Se ha confirmado que las directrices arquitectónicas, protecciones de caché (`stale-while-revalidate`), y los flujos read-path están en orden en todas las capas (Backend Core, Provider Bridge y PWA). Los endpoints conservan estrictamente el contrato JSON (`items` array y `syncState`), las pruebas de Node funcionan sin fallos y los builds de Vite se ejecutan correctamente. Adicionalmente, se corrigieron las respuestas de actualización y creación de Mongo en `upsertMessage` y `upsertChat` para garantizar que devuelvan el objeto `.toJSON()` en lugar del objeto nativo, cumpliendo estrictamente con las instrucciones de la memoria global para Mongoose. El sistema es estable y preserva su modelo canónico multi-canal.
- **ChatFix-Orchestrator**: Refactored provider adapters (`BaseAdapter` and `WhatsAppAdapter`) to strictly enforce proper scoping in their method signatures. Removed the `conversationId` parameter from global, account-scoped methods (like `listChats`, `fetchStatusDescriptors`, `markStatusRead`, `getChatAvatarUrl`, etc.) and appropriately adjusted their invocations in the central orchestrator (`backend/index.js`), ensuring architectural purity and preventing unnecessary parameter bloat.
- **ChatFix-AI-Ops**: Validated and clamped AI operation environment variables directly into process.env. Se refactorizó `validateStartupConfig` para que elimine comprobaciones redundantes y asigne incondicionalmente los valores seguros de `safeNumber`. También se establecieron explícitamente encabezados `Cache-Control: no-cache` en los endpoints de diagnóstico de IA (`/api/ai/config`, `/api/ai/health`, `/api/ai/models`) para evitar respuestas estancadas.
- **ChatFix-PWA-UX-Ops**: Se verificó la robustez operativa de la PWA. El almacenamiento en caché IndexedDB respeta estrictamente el contrato canónico (`prefix:provider:accountId:conversationId`), aislando correctamente proveedores y cuentas de múltiples entornos. La optimización UI optimista funciona con resiliencia tanto para borradores únicos como para encolados (queue logic), y la ergonomía móvil (touch targets 48x48) está asegurada.
- **Vision Agent**: Reportando progreso en el diseño premium del dashboard.
- **BackendCore**: Confirmadas protecciones de cache stale-while-revalidate. Contratos de SyncState y normalización Mongoose $set validados. Fix aplicado a las rutinas de safeUrl de la configuración inicial.
- **ErrorGuardian**: Monitoreando logs de error en producción.
- **ChatFix-UX-Accessibility-Trust**: Se mejoró la retroalimentación visual de los estados de sesión (`auth_failure`, `qr`, `disconnected`, etc.) y la accesibilidad de teclado (`focus-visible`) en el frontend PWA. Además, se añadieron ARIA roles a los banners de alerta, instrucciones QR y validación de estado de IA, y se previno explícitamente el uso de botones de acción cuando la aplicación está fuera de línea. Adicionalmente, se ajustaron tamaños y pesos de fuente de los banners de error de API para mejorar lectura, se previno desbordamiento de contenido en banners con flex-wrap, y se mejoró la locución del estado de conectividad para lectores de pantalla.
- **ChatFix-AI-Ops**: Se eliminaron las validaciones manuales redundantes de los límites de variables de entorno numéricas en `validateStartupConfig`, delegando completamente en la robustez comprobada de `safeNumber`. Se incorporaron validaciones de URL rigurosas usando `safeUrl` al arranque para asegurar que `LM_STUDIO_URL` y `CLOUDFLARE_AI_BASE_URL` tengan un formato válido (http/https). Adicionalmente se centralizaron las advertencias de seguridad de la API KEY reutilizando la constante en los middlewares, y se agregaron validaciones estrictas al endpoint `/api/ai/config` para asegurar que no se cambie a proveedores de IA (como Cloudflare) sin credenciales suficientes. Se garantizó también que `process.env.AI_PROVIDER` asigne exclusivamente `cloudflare` o `lmstudio` al estado canónico `DEFAULT_AI_CONFIG`, evitando estados inconsistentes si se pasa un valor no soportado. Documentación actualizada reflejando estos cambios en `README.md` y `backend/.env.example`.
- **ChatFix-Provider-Bridge**: Se estandarizó la inyección de parámetros canónicos (`provider`, `accountId`, `conversationId`) en las llamadas del orquestador a los adaptadores, protegiendo las firmas y evitando acoplamiento explícito en las capas superiores.
- **ChatFix-Backend-Core**: Se revisaron y confirmaron las protecciones de endpoints `GET /api/*`, la presencia del caché `stale-while-revalidate`, y el modelo canónico multicanal (índices compuestos, deduplicación). También se corrigió un bug en `getSyncStateSnapshot` para que retorne siempre `provider` y `accountId` para la UI.
- **ChatFix-Frontend-PWA**: Se eliminó la clase `.sr-only` de los labels de inputs de autenticación/búsqueda para hacerlos visibles cumpliendo con requerimientos de accesibilidad. Además, se implementó sugerencias de corrección de IA automáticas de fondo con `AbortController` al escribir borradores en el compositor.
- **ChatFix-PWA-UX-Ops**: Se agregó persistencia local a los estados archivados (`statusArchiveItems`). Ahora la aplicación hidrata estos datos instantáneamente desde IndexedDB usando las nuevas funciones `getCachedStatuses` y `setCachedStatuses` para un inicio fuera de línea sin bloqueos, antes de solicitar los datos frescos al servidor en segundo plano.
- **ChatFix-UX-Conversation-Flow**: Se mejoró significativamente el flujo conversacional eliminando la fricción entre la redacción y el envío rápido. Los botones de acción se agruparon unificadamente en `.composerActions` mediante props dinámicos para garantizar estabilidad absoluta en el layout sin unmounts condicionales. Se modificaron los botones y el evento "Enter" (`handleDraftKeyDown`) para que el estado de fondo `correcting` ya no bloquee la interacción, abortando transparentemente cualquier sugerencia pendiente vía `AbortController` al enviar. Adicionalmente, se mitigó la ambigüedad en el texto de entrada agregando la etiqueta "Tu borrador original" y barras `.syncProgressBar` para operaciones de fondo sin irrumpir en el compositor.
- **ChatFix-UX-Conversation-Flow (Iteración 2)**: Se eliminaron los modificadores `hideOnMobile` de los botones del compositor para garantizar claridad semántica constante según reglas de UX. Se reemplazó el contenedor flotante `activityStateBadge` integrando el estado de carga (spinner y `aria-busy`) directamente en los botones, optimizando el espacio vertical y re-etiquetando "Enviar original (sin IA)" a "Enviar original".
- **ChatFix-UX-Mobile-Polish**: Se mejoró la ergonomía de la interfaz móvil. Se agregaron íconos a la navegación del sidebar y se ocultó su texto tras `.hideOnMobile` para liberar espacio. En CSS, las transiciones del panel y sidebar y modales (como `slideUpMobile`) se hicieron más rápidas (0.25s) para mejorar la respuesta percibida, el ancho de los mensajes (`.bubble`) se aumentó al 90%, y los paddings de `.messagesArea` y `.composer` se redujeron para maximizar el área de lectura en pantallas pequeñas. También se ajustó el espaciado de los botones `.composerActions` a 8px para asegurar blancos táctiles precisos. Además, los botones del composer ahora usan el atributo `disabled` cuando la aplicación está en estado `correcting` para evitar interacciones accidentales y mantener la estabilidad del layout sin desmontarlos.
- **ChatFix-Architectural-Coordination**: Se corrigieron los índices compuestos de los modelos `Message`, `Chat` y `SyncState` en el backend para incluir `conversationId` y evitar violaciones de sintaxis (`1: 1`), preservando el aislamiento multi-cuenta. Adicionalmente, se actualizó la consulta de MongoDB en `upsertChat` para requerir el `conversationId`, y se corrigió `getSyncStateSnapshot` de manera que propague los atributos canónicos sobreescribiendo posibles atributos del caché local, asegurando el contrato hacia el frontend.
- **ChatFix-PWA-UX-Refinements**: Se corrigió el bug de contexto donde `replyTarget` no se limpiaba al cambiar de chat usando clics directos sobre los elementos en lugar de la tecla Escape. Además, se deshabilitaron correctamente los campos de texto durante el estado combinando de IA (`correctingAndSending`) previniendo race conditions en envíos rápidos.
- **ChatFix-AI-Ops (Final Validation)**: Se verificó exitosamente que las configuraciones de operaciones de IA en `backend/index.js`, `.env.example`, `docker-compose.yml`, `README.md` y `docs/OPERATIONS_RUNBOOK.md` cumplen con todas las restricciones de seguridad y validaciones numéricas/de URL esperadas (`safeNumber`, `safeUrl`), manteniendo total compatibilidad con LM Studio y Cloudflare. Los endpoints de diagnóstico devuelven el Cache-Control adecuado y los tokens sensibles están protegidos.
- **ChatFix-Frontend-PWA-UX**: Se auditó exhaustivamente la aplicación frontend y la caché IndexedDB, validando exitosamente que se cumple con la estructura de claves de caché canónica aislada (`prefix:provider:accountId:conversationId`). Se comprobó la correcta implementación y mantenimiento del UX y los requisitos operativos móviles como el uso de `flex-wrap` en alertas, persistencia correcta del `originalDraftLabel` sin montar y desmontar elementos y vaciado en el evento `onChange`, vaciado del `replyTarget` mediante la función `setReplyTarget(null)` en la navegación explícita y cierres de modales, inhabilitación correcta de la botonera (`disabled`) durante `isOffline === true` en acciones críticas, uso exclusivo de transiciones de `0.25s` para snappiness, inyección dinámica de `API_URL` en inicialización PWA, y manejo del estado mediante AbortController para prevenir race conditions. Los tests de frontend en node para `cacheStore.js` pasan adecuadamente confirmando la estricta adherencia a la arquitectura offline y caché multi-tenant sin que sea necesario introducir cambios perjudiciales.
- **ChatFix-UX-Conversation-Flow (Iteración 3)**: Se retiró el bloqueo de botones de composer que impedía el envío en el modo de `correcting` de fondo. Para asegurar legibilidad sin ocultar los textos en móviles, se optimizó `.composerActions button` a `flex: 1 1 100%` en viewport estrecho.
- **ChatFix-UX-Mobile-Polish (Layout & Interactions)**: Revisado y optimizado el layout y las microinteracciones en pantallas móviles (breakpoint 920px). Se mejoró la rapidez percibida ajustando las animaciones clave (slideUpMobile, slideIn, slideInToast, opacity/transform transitions) a duraciones rápidas de 0.25s. Se amplió el ancho de lectura de `.bubble` al 90% para mejor legibilidad. Se ajustaron los paddings de `.composer` y `.messagesArea` para maximizar el contenido visible y se aplicó layout de flex-wrap en los `.composerActions` forzándolos a `100%` para legibilidad sin usar `hideOnMobile`. Los touch targets garantizan 48x48px de tamaño mínimo.
- **ChatFix-Architectural-Coordination (Final Validation)**: Completada revisión arquitectónica general en todo el stack. Se validó que el orquestador (`backend/index.js`), los adaptadores (`WhatsAppAdapter`, `BaseAdapter`), y el cache frontend (`cacheStore.js`) se comuniquen estrictamente usando la tupla normalizada de proveedor, cuenta y conversación (`provider + accountId + conversationId`). Las respuestas de listados (chats y mensajes) exponen correctamente la estructura `items` más metadatos de sincronización asíncrona (con cola de retries offline/background), respetando al tiempo la directiva de endpoints enlazados con caché persistente (como IndexedDB) e inyectando siempre encabezados `stale-while-revalidate`. Adicionalmente, el modelo local de persistencia está protegido por la normalización segura del parser, la validación al startup de URLs y dependencias, y el estricto apego al read path asincrónico. Tests automatizados en backend se completaron sin fallos.
