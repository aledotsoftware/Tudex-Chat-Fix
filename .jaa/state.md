# JAA Global System State

Este archivo contiene el estado compartido entre todos los repositorios gestionados por JAA.
Los agentes pueden leer este estado para entender el contexto de otros proyectos.

## 🚀 ACTIVE MILESTONES
- [JAA] Implementación de Jerarquía de Contexto (.jaa.md global) - **COMPLETADO**
- [JAA] Sistema de Estado Global (system-state.md) - **EN PROCESO**
- [GENERAL] Estandarización de agentes para todos los repositorios.

## 📝 AGENT NOTES
- **Vision Agent**: Reportando progreso en el diseño premium del dashboard.
- **ErrorGuardian**: Monitoreando logs de error en producción.
- **ChatFix-Orchestrator**: Inicializó la arquitectura base.
- **ChatFix-Backend-Core (Iteración 1)**: Añadidos los modelos de datos canónicos (`Chat`, `Message`, `SyncState`) requeridos para la arquitectura de mensajería centralizada en `backend/index.js`, asegurando indexación compuesta correcta por `provider`, `accountId` y `conversationId`.
- **ChatFix-Backend-Core (Iteración 2)**: Implementada lógica de cache multi-nivel (L1 en memoria con TTL, L2 persistente) para lectura de chats y mensajes con invalidación selectiva, cumpliendo el contrato `items + syncState` para lecturas sin latencia de proveedor.
- **ChatFix-Backend-Core (Iteración 3)**: Implementada la infraestructura de Sync Asíncrono (`stale-while-revalidate`) en `backend/index.js`, incluyendo deduplicación de tareas, limitación de concurrencia para evitar la inanición del Event Loop, cola en memoria coordinada por Mongoose, y refactorización completa del proceso de actualización de mensajes para mantener siempre la normalización canónica multi-proveedor.
- **ChatFix-Provider-Bridge**: Implementada la base extensible multi-canal con `base-adapter.js`, `provider-registry.js` y el primer adaptador operativo `whatsapp-adapter.js`. El frontend ahora puede consultar información agnósticamente del canal subyacente porque toda data fue envuelta y expuesta acorde al contrato estricto de adaptadores (e.g. `isReady`, `getStatus`, `listChats`, `fetchMessages`, `markRead`).
- **ChatFix-Frontend-PWA (Iteración 4)**: Se implementó la capa de almacenamiento IndexedDB local en `frontend/src/cacheStore.js` con soporte para almacenar y recuperar chats, mensajes, y status bajo el formato jerárquico `provider:accountId:conversationId`.
- **ChatFix-Frontend-PWA (Iteración 5)**: Actualizada la aplicación principal (`frontend/src/App.jsx`) para integrarse de forma completa con la arquitectura asíncrona (stale-while-revalidate), leyendo prioritariamente de `IndexedDB` e hidratando el estado de conexión transparentemente mediante la respuesta encolada del backend (e.g., sincronización en segundo plano con indicadores no bloqueantes).
- **ChatFix-UX-Conversation-Flow (Iteración 6)**: Se refactorizó la interfaz móvil PWA (`frontend/src/App.css` y `App.jsx`) para desvincular estructuralmente el componente de Estados (Statuses) del Layout Principal. Se centralizó la lógica bajo `viewMode`, garantizando transiciones de pantalla inmediatas (sin retardos generados por red) e incorporando estados vacíos, avatares consistentes y un diseño adaptable a dark/light mode con una barra sincronizadora persistente y confiable (`syncProgressBar`) que respeta las normas de retroalimentación asíncrona. Además, todos los tests pasaron y los constraints operativos continúan satisfechos.
- **ChatFix-UX-Conversation-Flow (Iteración 7)**: Se mejoró la UI visual de los botones de envío en la botonera de `composerActions`. El botón de envío mejorado con IA (`.sendCorrectedBtn`) ahora tiene un gradiente lineal (`linear-gradient(135deg, var(--accent-primary) 0%, #3a7bd5 100%)`) y un `box-shadow` (`0 4px 12px rgba(0, 210, 255, 0.3)`), distinguiéndose claramente del botón secundario plano (`.plainSendBtn`). Se verificó que las arquitecturas PWA, caché local IndexedDB, y los demás constraints de UX operativa como `syncProgressBar` permanezcan sin alteraciones y completamente en orden con las directrices globales.
- **ChatFix-UX-Conversation-Flow (Iteración 8)**: Se consolidó la resiliencia y claridad de la interfaz de chat (App.jsx y App.css) corrigiendo el comportamiento de optimismo (Optimistic UI). Cuando una petición de envío de mensaje falla, la aplicación ahora concatena con cuidado los borradores guardados, evitando el sobreescritura accidental del texto que el usuario haya escrito mientras la operación asincrónica estaba en curso. También se habilitó la escritura concurrente mediante la eliminación de la restricción `disabled` global del textarea principal, fomentando una verdadera operación multihilo PWA. Adicionalmente, se corrigió una directiva anti-patrón de CSS que imponía un `display: flex` problemático en todos los contenedores de texto, limitándolo exclusivamente al `.chatHeader h3` (para arreglar un problema con badges sincronizadores y truncación). La botonera `.composerActions` ahora distingue inequívocamente su contexto de botones con selectores de especificidad estricta y evita el uso de `!important`.
- **ChatFix-UX-Accessibility-Trust (Iteración Final)**: Se refactorizó la semántica y retroalimentación de los estados de conexión en el frontend (App.jsx y App.css) para potenciar la accesibilidad, el confort visual continuo y la navegación por teclado. Específicamente, se migró el rol y el comportamiento dinámico (`role="alert" aria-live="assertive"` a `role="status" aria-live="polite"`) en banners no críticos persistentes (offlineBanner, updateBanner, warningBanner) para evitar interrupciones disruptivas en lectores de pantalla. Se garantizó, en contraste, que la alerta crítica de llave de acceso (`apiKeyError`) conserve su rol de alerta estricto (`role="alert"`). A nivel visual, se amplió el foco de navegación (`outline`) de 3px a 4px de grosor en todos los elementos interactivos, y se optimizó el contraste para el texto de avisos en modo oscuro, ajustando `.notice.error` a un rojo más brillante (`#ff8a8a`) y `.notice.warning` a un amarillo más legible (`#ffe066`). Se consolidó la estructura de estilos con una clase aislada `.syncDisclaimer`. Todos los tests frontend locales pasaron y la verificación visual mediante Playwright no detectó regresiones de formato, alineación, ni contraste.
- **ChatFix-Architectural-Coordination (Final Validation)**: Completada la revisión arquitectónica final. Se verificó que el modelo canónico (`provider + accountId + conversationId`) se aplica consistentemente en todos los componentes del sistema, incluyendo los modelos de Mongoose (`Chat`, `Message`, `SyncState`), el manejo de caché (`getStorageKey`, `getSyncStateSnapshot`, `invalidateMessagesCache`, etc.), y las firmas de los adaptadores (`BaseAdapter`, `WhatsAppAdapter`). Los endpoints del backend para lectura y modificación (`/api/chats`, `/api/chats/:chatId/messages`, `/api/chats/:chatId/read`, `/api/send`) respetan estrictamente el contrato, garantizando el aislamiento de contexto en un entorno multi-proveedor sin regresiones funcionales. Adicionalmente, el frontend mantiene optimismo en UI, integridad de la sesión, y resiliencia offline. Los tests correspondientes, tanto en frontend como backend, se ejecutaron exitosamente.

- **ChatFix-Backend-Core (Global Init Fix)**: Fix to properly call `safeNumber` right on global declarations (`STATUS_POLL_INTERVAL_MS`, `AVATAR_TTL_MS`, etc.) at the top of `backend/index.js` to ensure the values are validated before the execution flow reaches startup methods. Also, explicitly check `typeof targetChatId !== 'string'` in `WhatsAppAdapter.sendMessage` to prevent runtime TypeError.
- **ChatFix-Provider-Bridge (Verification)**: Verified `WhatsAppAdapter`, `BaseAdapter`, and `ProviderRegistry` implementations. The adapter correctly respects the multi-channel fallback logic (`chatId || conversationId`), safely parses IDs, implements all required methods (like `isReady`, `getStatus`, `listChats`, `fetchMessages`, `markRead`, `getMessageById`), and does not directly access provider-specific fields like `_serialized` outside of the extraction methods (`extractMessageContext`, `extractChatContext`, etc). Tested backend successfully with passing tests.
- **ChatFix-UX-Mobile-Polish**: Added missing `setReplyTarget(null);` to the mobile back button (`.mobileBackBtn`) inside the `.statuses` view in `frontend/src/App.jsx`. This strictly enforces state cleanup and prevents phantom reply targets from leaking into the global component state when switching contexts in the PWA. Verified other constraints including IndexedDB cache storage keys (`provider`, `accountId`, `conversationId`), dynamic API resolution, offline state capabilities (text inputs un-disabled), and accessibility rules. Built and tested frontend successfully.
- **ChatFix-UX-Conversation-Flow (Iteración 9 - Claridad)**: Se resolvió la ambigüedad en el compositor de mensajes trasladando los atajos de teclado (`Enter` vs `Ctrl+Enter`) desde el placeholder del textarea hacia un contenedor estático `.composerHeader` con la clase `.composerShortcutsHint`, garantizando que las instrucciones permanezcan visibles mientras el usuario escribe. Adicionalmente, se corrigieron los layouts de los títulos `.chatHeader h3` y `.sidebarHeader h2` envolviéndolos en contenedores flex (`.chatTitleRow`, `.sidebarTitleRow`) para permitir una correcta truncación de texto (`text-overflow: ellipsis`) sin superponerse o romper los badges de sincronización. Finalmente, se añadieron íconos claros (💡, ↩️) a las etiquetas `.queuedReplyLabel` y `.replyTargetLabel` para mejorar la diferenciación visual entre borradores originales, sugerencias de IA, y respuestas en cola. Se validó visualmente mediante Playwright y todos los tests pasan correctamente.
- **ChatFix-UX-Mobile-Polish (Iteración 10 - Microinteracciones Mobile)**: Se mejoró la ergonomía táctil (affordances) agregando una regla global CSS (`min-width: 48px`, `min-height: 48px`) para elementos interactivos como botones, inputs y selects en `App.css`. Además se eliminaron las declaraciones redundantes previas en los media-queries, optimizando y centralizando la declaración. Adicionalmente, el frontend mantiene un flujo de layouts en transiciones móviles donde `viewMode === "statuses"` abre el panel de chat `.chatOpen` inmediatamente (sin saltos), comportamiento que se verificó directamente. Los scripts de Playwright se validaron en el puerto 5173 para reflejar los cambios PWA correctos. Todos los tests pasan correctamente (front y back).
- **ChatFix-UX-Accessibility-Trust (Final Refinements)**: Further refined connection feedback accessibility in `App.jsx` by explicitly setting `role="status"` and `aria-live="polite"` on the `reconnectingAlert` spinner to prevent unnecessary screen reader urgency. Ensured that `apiKeyError` and `aiHealthAlert` maintain critical `role="alert"`. Addressed the CSS contrast on `.notice.error.session-rejected` to match the `#ff8a8a` text against `rgba(255, 82, 82, 0.2)` background standard for dark mode readability. Systematically cleaned up redundant 48x48px media query breakpoints in `App.css` and replaced them with a single global CSS declaration to guarantee a minimum touch target size of 48x48px across all devices for all interactive inputs and buttons, honoring mobile affordances. Tests running successfully.
- **ChatFix-Architectural-Coordination (Final Overall Validation)**: Fully validated the entire backend and frontend architecture against the `provider/accountId/conversationId` paradigm constraints.
  - Verified `App.jsx` handles local `setReplyTarget(null)` efficiently in `mobileBackBtn` for status view and everywhere else context is switched.
  - Ensured `.syncProgressBar` relies directly within `.chatHeader` and `.sidebarHeader` as requested.
  - Checked `backend/index.js` explicitly invokes `invalidateMessagesCache` and other caches matching canonical schemas parameters order.
  - Validated Mongoose Models explicitly define canonical architectures limits (like `unique: true`).
  - Provider Bridges gracefully accept generic payloads resolving missing variables locally ensuring backward compatibility for legacy callers. All systems pass full regression analysis without unexpected issues.
  - Verified frontend API payloads correctly define canonical architecture identifiers in `/api/send`.
- **ChatFix-Provider-Bridge (Iteración Bridge de Proveedor)**: Se reforzó el adapter `WhatsAppAdapter` implementando manejo de fallbacks a strings puros en todos los métodos extract (`extractMessageContext`, `extractChatContext`, `extractStatusDescriptor`) para asegurar flexibilidad con futuros canales de mensajería (multi-canal). Se agregaron guardas de validación (`early exits`) en utilidades como `hasMedia`, `hasQuotedMsg` y `getChatIdFromMessage` (`if (!message) return false/null;`) evitando `TypeError`s inesperados. Se verificó que `ProviderRegistry`, y los flujos asíncronos (`send`, `read`, `listChats`) mantienen el contrato `provider + accountId + conversationId` sin modificaciones problemáticas. Se ejecutaron y pasaron todos los tests.
- **ChatFix-Backend-Core (Iteración Fallback)**: Se agregaron variables como 'conversationId' en el fallback de extracción del endpoint `/api/send` y como retorno en dicho endpoint y en `/api/chats/:chatId/read`. Esto asegura un correcto soporte al nuevo paradigma 'provider+accountId+conversationId'.
- **ChatFix-Provider-Bridge (Validación Final)**: Confirmada la robustez de `WhatsAppAdapter`, `BaseAdapter` y `ProviderRegistry`. Los métodos de extracción (`extractMessageContext`, `extractChatContext`, `extractStatusDescriptor`) manejan correctamente fallbacks a strings puros garantizando compatibilidad multicanal. Las validaciones de `early exits` (ej. `if (!message) return null;`) en utilidades como `hasMedia`, `getChatIdFromMessage` y otros métodos del adaptador están intactas. Los flujos de asincronía y sincronización respetan el contrato canónico estricto (`provider + accountId + conversationId`). Todos los tests de la capa de proveedor y de backend pasan exitosamente.
- **ChatFix-UX-Operations-Polish**: Solucionado el bug que dejaba la interfaz bloqueada ("Mejorando y enviando...") cuando se abortaba una solicitud de corrección de IA en curso, asegurando que `setCorrectingAndSending(false)` se llame explícitamente en todos los flujos de interrupción (`AbortController`). Se agregó también la instancia `AbortController` faltante al flujo `correctAndSend` para permitir la cancelación por parte del usuario. Se corrigió el espaciado del input de contraseña en el login elevando el `padding-right` a `60px` para evitar solapamiento con el ícono de visibilidad, respetando las normas de accesibilidad y UI establecidas. Las validaciones locales y tests (frontend y backend) pasan exitosamente.
